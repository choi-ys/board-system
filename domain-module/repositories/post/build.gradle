apply plugin: "org.springframework.boot"
apply plugin: "io.spring.dependency-management"

bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation project(":domain-module:domain-mysql")
}